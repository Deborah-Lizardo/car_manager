#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "../include/list.h"

// Function to insert a car into the car list
Node* insertAtEnd (Node* list, Car car);
Node* newNode = (Node*)malloc(sizeof(newNode));
if (newCarNode == NULL) {
    printf("Memory allocation failed!\n");
    return list;